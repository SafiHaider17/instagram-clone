<%= form_with(url: posts_path, method: "get", local: true) do |form| %>
  <%= form.text_field :search, value: params[:search], placeholder: "Search Post", class: 'form-control me-2' %>
  <%#= form.submit "Search" %>
<% end %>
